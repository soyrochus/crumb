{
  "name": "crumb",
  "version": "0.9.0",
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  },
  "private": true,
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run scripts/build.ts",
    "build:native": "bun run scripts/build-native.ts",
    "build:ui": "bun run scripts/build-ui.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "verify:performance": "bun run scripts/verify-performance.ts",
    "verify:readonly": "bun run scripts/verify-readonly.ts"
  },
  "type": "module",
  "dependencies": {
    "@nativewindow/webview": "1.0.6"
  }
}
